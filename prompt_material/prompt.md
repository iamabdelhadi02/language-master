```
Describe this onboarding screen image in exact detail so I can implement it pixel-perfectly in React Native (Expo + NativeWind). Output in this exact format — no intro, no commentary:

## Layout (top to bottom, numbered)
For each element: type (text/image/button/indicator), content, exact positioning, width constraints

## Spacing
Every gap between elements in px or approximate (close/tight/large)

## Typography
Per text element: font family (Poppins-*), weight, size in px, line height if visible, alignment

## Colors
Every hex value you see: background, text, buttons, icons, dividers, dots — both active and inactive states

## Imagery
Describe illustrations/mascots: what they depict, their container shape, shadows, approximate size ratio

## Interactions
Any buttons, links, tappable areas, swipe indicators

Be exhaustive. If unsure about a value, mark it with [ESTIMATE] and give your best guess.
```